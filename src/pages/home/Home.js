import React, { useEffect, useState } from 'react';

function Home() {
    
//{sessionInformation.valid === false ? <Main sessionInformation={sessionInformation} setSessionInformation={setSessionInformation}/> : <Teste/>}
    return ( 
        <>
            <h1>Home</h1>
        </>
    )
}
export default Home;
